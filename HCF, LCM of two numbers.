//GCD, LCM of two numbers.
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int a,b;
    cin>>a>>b;
    int hcf,lcm;
    hcf=__gcd(a,b);
    lcm=(a*b)/hcf;
    
    cout<<"hcf is : "<<hcf<<" "<<" lcm is : "<<lcm;
    return 0;
}
